# halium-generic-adaptation-build-tools, cloned by build.sh
/build/

# Build outputs
/workdir/
/out/
/ota/
/images/

# Release build scratch: downloaded upstream tarballs and built images.
# 800 MB+ of rootfs/GSI artifacts land here; never commit them.
dl/
